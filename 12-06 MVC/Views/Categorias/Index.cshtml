@model IEnumerable<Aula_12_06_MVC.Models.Categoria>
@{
    ViewBag.Title = "Index";
}

<h2>Categorias</h2>

@*@foreach (string cat in ViewBag.ListaCategorias)
{
    //<p>cat</p>
    ///<a href="#">@cat</a>
    @Html.ActionLink(@cat, "Index","Categorias")
    @: |
}*@

@foreach(var cat in Model)
{
    <dl>
        <dt>@cat.Nome</dt>
        @if (!string.IsNullOrEmpty(cat.Descricao))
        {
        <dd>@cat.Descricao</dd>
        }
        else
        {
            <dd><em>Sem Descrição</em></dd>
        }

    </dl>